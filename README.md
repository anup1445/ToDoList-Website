# ToDoList-Website
This is a simple web application for creating and managing to-do lists. It allows users to add tasks, mark them as completed, and remove them from the list. The project is built using Node.js and Express for the server-side logic, while the client-side functionality is implemented using HTML, CSS, and JavaScript.
